# NASDAQ SENTINEL WORKFLOW - V6 AUTOMATIZACION ROBUSTA 2026-09-01
name: Nasdaq Sentinel Cloud

on:
  workflow_dispatch:
  schedule:
    # GitHub ejecuta cada cinco minutos de lunes a viernes.
    # bot_cloud.py decide internamente si la sesion de Nueva York esta abierta.
    # Se evitan los minutos 00 y 05 para reducir retrasos por saturacion.
    - cron: "2-57/5 * * * 1-5"

permissions:
  contents: write

concurrency:
  group: nasdaq-sentinel-cloud
  cancel-in-progress: false

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 4

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Show execution type
        run: echo "Execution type $GITHUB_EVENT_NAME"

      - name: Run Nasdaq Sentinel
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_CHAT_ID_HIJO: ${{ secrets.TELEGRAM_CHAT_ID_HIJO }}
          TELEGRAM_WEBHOOK_ACTIVO: "1"
        run: python bot_cloud.py

      - name: Save state and signals
        run: |
          git config user.name "Nasdaq Sentinel Cloud"
          git config user.email "actions@users.noreply.github.com"

          git add estado_cloud.json 2>/dev/null || true
          git add senales_simuladas.csv 2>/dev/null || true

          if ! git diff --cached --quiet; then
            git commit -m "Actualizar el estado de Nasdaq Sentinel"
            git pull --rebase
            git push
          fi
